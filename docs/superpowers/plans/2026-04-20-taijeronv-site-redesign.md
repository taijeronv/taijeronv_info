# taijeronv.info Site Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-page Astro landing page showcasing TJ Taijeron's AI-Integrator work, replacing the outdated Hugo/Academic site, deployed to Netlify.

**Architecture:** Astro static site with Tailwind CSS. All content lives in typed data files (`src/content/projects/` and `src/data/skills.ts`) — editable without touching component code. Five section components assembled in a single `index.astro` page.

**Tech Stack:** Astro, Tailwind CSS, Netlify, Node 20

---

## File Map

| File | Responsibility |
|------|---------------|
| `astro.config.mjs` | Astro config with Tailwind integration |
| `netlify.toml` | Build command + publish dir |
| `tailwind.config.mjs` | Tailwind config (dark mode, content paths) |
| `src/pages/index.astro` | Root page — imports all section components, sets HTML head/SEO |
| `src/styles/global.css` | Tailwind base directives only |
| `src/components/Hero.astro` | Name, title, tagline, two CTAs |
| `src/components/About.astro` | 2-paragraph bio, availability note |
| `src/components/Skills.astro` | Three-column skills grid, reads from `skills.ts` |
| `src/components/Projects.astro` | Three project cards, reads from content collection |
| `src/components/Contact.astro` | Email CTA, availability note |
| `src/content/config.ts` | Astro content collection schema for projects |
| `src/content/projects/cade.md` | CADE project data |
| `src/content/projects/deck-builder.md` | Deck Builder project data |
| `src/content/projects/orders-production.md` | Orders Production project data |
| `src/data/skills.ts` | Skills data (name + list of uses per tool) |
| `public/avatar.jpg` | Profile photo (copied from existing site) |

---

## Task 1: Scaffold Astro Project

**Files:**
- Create: `astro.config.mjs`
- Create: `tailwind.config.mjs`
- Create: `src/styles/global.css`
- Create: `package.json` (generated by scaffold)

- [ ] **Step 1: Scaffold Astro with minimal template**

Run from `/Users/redleg/_repos/ai-workspaces/taijeronv-info/`:
```bash
npm create astro@latest . -- --template minimal --no-install --no-git
```
When prompted about existing files: choose to ignore/continue.

- [ ] **Step 2: Install dependencies**

```bash
npm install
```
Expected: `node_modules/` created, no errors.

- [ ] **Step 3: Add Tailwind integration**

```bash
npx astro add tailwind --yes
```
Expected: `tailwind.config.mjs` created, `astro.config.mjs` updated with Tailwind integration.

- [ ] **Step 4: Verify tailwind.config.mjs content**

File should contain:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

- [ ] **Step 5: Replace src/styles/global.css with Tailwind directives**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 6: Verify dev server starts**

```bash
npm run dev
```
Expected: `http://localhost:4321` opens, shows Astro default page. Stop server with Ctrl+C.

- [ ] **Step 7: Commit**

```bash
git add astro.config.mjs tailwind.config.mjs package.json package-lock.json src/styles/global.css
git commit -m "feat: scaffold Astro + Tailwind"
```

---

## Task 2: Configure Netlify

**Files:**
- Modify: `netlify.toml` (at repo root — replace Hugo config)

- [ ] **Step 1: Replace netlify.toml**

Overwrite `/Users/redleg/_repos/ai-workspaces/taijeronv-info/netlify.toml` with:
```toml
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.deploy-preview]
  command = "npm run build"

[context.branch-deploy]
  command = "npm run build"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
```

- [ ] **Step 2: Verify build succeeds**

```bash
npm run build
```
Expected: `dist/` directory created, no errors.

- [ ] **Step 3: Preview built site**

```bash
npm run preview
```
Expected: site serves from `dist/` at `http://localhost:4321`. Stop with Ctrl+C.

- [ ] **Step 4: Commit**

```bash
git add netlify.toml
git commit -m "feat: configure Netlify for Astro build"
```

---

## Task 3: Content Collection Schema + Project Files

**Files:**
- Create: `src/content/config.ts`
- Create: `src/content/projects/cade.md`
- Create: `src/content/projects/deck-builder.md`
- Create: `src/content/projects/orders-production.md`

- [ ] **Step 1: Create content collection schema**

`src/content/config.ts`:
```typescript
import { defineCollection, z } from 'astro:content';

const projects = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string(),
    order: z.number(),
    bullets: z.array(z.string()),
  }),
});

export const collections = { projects };
```

- [ ] **Step 2: Create CADE project file**

`src/content/projects/cade.md`:
```markdown
---
title: "CADE — Combined Arms Decision Exercise"
description: "A consequence-based decision exercise designed for Ukrainian staff officer training under real operational constraints."
order: 1
bullets:
  - "Built the full AI pipeline: scenario → OPORD with annexes → runbook → controller package"
  - "Used Claude, Gemini, and NotebookLM as drafting and synthesis engines with human validation at each stage"
  - "Validated 29–30 January 2026; exercise earned institutional approval for repeat execution"
---
```

- [ ] **Step 3: Create Deck Builder project file**

`src/content/projects/deck-builder.md`:
```markdown
---
title: "Deck Builder"
description: "AI-powered briefing generator for military planning products."
order: 2
bullets:
  - "Input: Markdown slide script. Output: mission-ready PPTX and HTML briefings"
  - "Includes AI skills for scripting slide content from source documents"
  - "Brand-standard templates for decision briefs, instruction briefs, and OPORD briefings"
---
```

- [ ] **Step 4: Create Orders Production project file**

`src/content/projects/orders-production.md`:
```markdown
---
title: "Orders Production"
description: "Streamlit GUI backed by the Claude API for drafting military planning documents."
order: 3
bullets:
  - "Three-tool suite: OPORD Writer, Narrative Writer, and Decision Support Matrix generator"
  - "Includes linting and validation scripts to enforce doctrinal formatting standards"
  - "Built against US Army doctrine (FM 5-0, FM 6-0) and NATO standards (AJP-5, NATO-COPD)"
---
```

- [ ] **Step 5: Verify build still passes**

```bash
npm run build
```
Expected: builds without errors.

- [ ] **Step 6: Commit**

```bash
git add src/content/
git commit -m "feat: add project content collection"
```

---

## Task 4: Skills Data File

**Files:**
- Create: `src/data/skills.ts`

- [ ] **Step 1: Create skills data file**

`src/data/skills.ts`:
```typescript
export const skills = [
  {
    name: "Claude Code / Cowork",
    uses: [
      "Auditing and mission-critical planning",
      "Content writing and deck generation",
      "AI-assisted document production",
    ],
  },
  {
    name: "Gemini / NotebookLM",
    uses: [
      "Document conversion and discovery",
      "Deep research and information synthesis",
      "Initial project planning and build specs",
    ],
  },
  {
    name: "Python + Streamlit + Claude API",
    uses: [
      "GUI tool development",
      "Automation pipelines",
      "Structured document generation",
    ],
  },
];
```

- [ ] **Step 2: Commit**

```bash
git add src/data/skills.ts
git commit -m "feat: add skills data"
```

---

## Task 5: Hero Component

**Files:**
- Create: `src/components/Hero.astro`

- [ ] **Step 1: Create Hero component**

`src/components/Hero.astro`:
```astro
---
---
<section id="hero" class="min-h-screen flex items-center justify-center bg-gray-950 text-white px-6">
  <div class="max-w-3xl text-center">
    <h1 class="text-5xl font-bold tracking-tight mb-3">
      Vincent "TJ" Taijeron
    </h1>
    <p class="text-xl text-blue-400 font-medium mb-6">AI-Integrator</p>
    <p class="text-2xl text-gray-300 leading-relaxed mb-10">
      I build AI systems that make military planning faster, clearer, and executable under pressure.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a
        href="#projects"
        class="bg-blue-600 hover:bg-blue-500 text-white px-8 py-3 rounded-lg font-medium transition-colors"
      >
        View My Work
      </a>
      <a
        href="#contact"
        class="border border-gray-600 hover:border-gray-400 text-gray-300 hover:text-white px-8 py-3 rounded-lg font-medium transition-colors"
      >
        Get In Touch
      </a>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Wire Hero into index.astro temporarily to verify**

Replace `src/pages/index.astro` with:
```astro
---
import Hero from '../components/Hero.astro';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TJ Taijeron</title>
  </head>
  <body class="bg-gray-950">
    <Hero />
  </body>
</html>
```

- [ ] **Step 3: Start dev server and verify Hero renders**

```bash
npm run dev
```
Open `http://localhost:4321`. Verify: name displays, tagline displays, both buttons visible, dark background. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/Hero.astro src/pages/index.astro
git commit -m "feat: Hero component"
```

---

## Task 6: About Component

**Files:**
- Create: `src/components/About.astro`

- [ ] **Step 1: Create About component**

`src/components/About.astro`:
```astro
---
---
<section id="about" class="py-24 px-6 bg-gray-900 text-white">
  <div class="max-w-3xl mx-auto">
    <h2 class="text-3xl font-bold mb-8 text-white">About</h2>
    <div class="space-y-4 text-gray-300 text-lg leading-relaxed">
      <p>
        Field Artillery veteran with 38 years of service. Originally from Guam, I relocated to Ukraine in 2013 and have lived and worked there since.
      </p>
      <p>
        I currently serve as AI-Integrator at JMTG-U, where I design and build AI-assisted planning tools for multinational military training audiences — including Ukrainian staff officers training under real operational constraints.
      </p>
      <p class="text-blue-400 font-medium">
        Available for remote roles beginning Fall 2026.
      </p>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Add About to index.astro**

```astro
---
import Hero from '../components/Hero.astro';
import About from '../components/About.astro';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TJ Taijeron</title>
  </head>
  <body class="bg-gray-950">
    <Hero />
    <About />
  </body>
</html>
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```
Scroll past Hero — verify About section renders with correct text. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/About.astro src/pages/index.astro
git commit -m "feat: About component"
```

---

## Task 7: Skills Component

**Files:**
- Create: `src/components/Skills.astro`

- [ ] **Step 1: Create Skills component**

`src/components/Skills.astro`:
```astro
---
import { skills } from '../data/skills';
---
<section id="skills" class="py-24 px-6 bg-gray-950 text-white">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-3xl font-bold mb-12">AI Skills</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {skills.map((skill) => (
        <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
          <h3 class="text-lg font-semibold text-blue-400 mb-4">{skill.name}</h3>
          <ul class="space-y-2">
            {skill.uses.map((use) => (
              <li class="text-gray-400 text-sm flex items-start gap-2">
                <span class="text-blue-600 mt-0.5 shrink-0">▸</span>
                <span>{use}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
```

- [ ] **Step 2: Add Skills to index.astro**

```astro
---
import Hero from '../components/Hero.astro';
import About from '../components/About.astro';
import Skills from '../components/Skills.astro';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TJ Taijeron</title>
  </head>
  <body class="bg-gray-950">
    <Hero />
    <About />
    <Skills />
  </body>
</html>
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```
Scroll to Skills — verify three columns render, each with correct tool name and use list. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/Skills.astro src/pages/index.astro
git commit -m "feat: Skills component"
```

---

## Task 8: Projects Component

**Files:**
- Create: `src/components/Projects.astro`

- [ ] **Step 1: Create Projects component**

`src/components/Projects.astro`:
```astro
---
import { getCollection } from 'astro:content';
const allProjects = await getCollection('projects');
const projects = allProjects.sort((a, b) => a.data.order - b.data.order);
---
<section id="projects" class="py-24 px-6 bg-gray-900 text-white">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-3xl font-bold mb-12">Projects</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {projects.map((project) => (
        <div class="bg-gray-800 border border-gray-700 rounded-xl p-6 flex flex-col">
          <h3 class="text-lg font-semibold text-white mb-2">{project.data.title}</h3>
          <p class="text-gray-400 text-sm mb-4">{project.data.description}</p>
          <ul class="space-y-3 mt-auto">
            {project.data.bullets.map((bullet) => (
              <li class="text-gray-300 text-sm flex items-start gap-2">
                <span class="text-blue-400 mt-0.5 shrink-0">▸</span>
                <span>{bullet}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>
```

- [ ] **Step 2: Add Projects to index.astro**

```astro
---
import Hero from '../components/Hero.astro';
import About from '../components/About.astro';
import Skills from '../components/Skills.astro';
import Projects from '../components/Projects.astro';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TJ Taijeron</title>
  </head>
  <body class="bg-gray-950">
    <Hero />
    <About />
    <Skills />
    <Projects />
  </body>
</html>
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```
Scroll to Projects — verify three cards render in order (CADE, Deck Builder, Orders Production), each with title, description, and bullets. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/Projects.astro src/pages/index.astro
git commit -m "feat: Projects component"
```

---

## Task 9: Contact Component

**Files:**
- Create: `src/components/Contact.astro`

- [ ] **Step 1: Create Contact component**

`src/components/Contact.astro`:
```astro
---
---
<section id="contact" class="py-24 px-6 bg-gray-950 text-white">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-3xl font-bold mb-4">Get In Touch</h2>
    <p class="text-gray-400 mb-8 text-lg">
      Open to remote roles globally. Available Fall 2026.
    </p>
    <a
      href="mailto:vincent.taijeron@gmail.com"
      class="inline-block text-blue-400 hover:text-blue-300 text-xl font-medium transition-colors border-b border-blue-400 hover:border-blue-300 pb-0.5"
    >
      vincent.taijeron@gmail.com
    </a>
  </div>
</section>
```

- [ ] **Step 2: Complete index.astro with all components and SEO**

Replace `src/pages/index.astro` with the final version:
```astro
---
import Hero from '../components/Hero.astro';
import About from '../components/About.astro';
import Skills from '../components/Skills.astro';
import Projects from '../components/Projects.astro';
import Contact from '../components/Contact.astro';
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Vincent 'TJ' Taijeron — AI-Integrator building AI systems for military planning. Available for remote roles beginning Fall 2026."
    />
    <meta property="og:title" content='Vincent "TJ" Taijeron — AI-Integrator' />
    <meta
      property="og:description"
      content="I build AI systems that make military planning faster, clearer, and executable under pressure."
    />
    <meta property="og:type" content="website" />
    <title>Vincent "TJ" Taijeron — AI-Integrator</title>
  </head>
  <body class="bg-gray-950 antialiased">
    <Hero />
    <About />
    <Skills />
    <Projects />
    <Contact />
  </body>
</html>
```

- [ ] **Step 3: Verify full page in browser**

```bash
npm run dev
```
Scroll through all five sections. Verify: Hero → About → Skills → Projects → Contact all render correctly with no layout breaks. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/Contact.astro src/pages/index.astro
git commit -m "feat: Contact component and complete index page"
```

---

## Task 10: Copy Avatar Image

**Files:**
- Create: `public/avatar.jpg`

- [ ] **Step 1: Copy existing avatar into Astro public directory**

```bash
cp /Users/redleg/_repos/ai-workspaces/taijeronv-info/taijeronv_info/content/authors/admin/avatar.jpg \
   /Users/redleg/_repos/ai-workspaces/taijeronv-info/public/avatar.jpg
```

- [ ] **Step 2: Add avatar to About component**

Edit `src/components/About.astro` — update the component to include the photo:
```astro
---
---
<section id="about" class="py-24 px-6 bg-gray-900 text-white">
  <div class="max-w-3xl mx-auto">
    <div class="flex flex-col sm:flex-row items-start gap-8 mb-8">
      <img
        src="/avatar.jpg"
        alt="Vincent TJ Taijeron"
        class="w-28 h-28 rounded-full object-cover shrink-0 border-2 border-gray-700"
      />
      <div>
        <h2 class="text-3xl font-bold mb-4 text-white">About</h2>
        <div class="space-y-4 text-gray-300 text-lg leading-relaxed">
          <p>
            Field Artillery veteran with 38 years of service. Originally from Guam, I relocated to Ukraine in 2013 and have lived and worked there since.
          </p>
          <p>
            I currently serve as AI-Integrator at JMTG-U, where I design and build AI-assisted planning tools for multinational military training audiences — including Ukrainian staff officers training under real operational constraints.
          </p>
          <p class="text-blue-400 font-medium">
            Available for remote roles beginning Fall 2026.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
```

- [ ] **Step 3: Verify avatar renders in browser**

```bash
npm run dev
```
Scroll to About — verify photo appears alongside bio text. Stop server.

- [ ] **Step 4: Commit**

```bash
git add public/avatar.jpg src/components/About.astro
git commit -m "feat: add profile photo to About section"
```

---

## Task 11: Mobile Responsiveness Review

**Files:** No new files — verify existing components.

- [ ] **Step 1: Start dev server**

```bash
npm run dev
```

- [ ] **Step 2: Check Hero on mobile**

In browser DevTools (F12), toggle device toolbar and select iPhone 14 (390px wide). Verify:
- Name and tagline are readable (not truncated)
- Both buttons stack vertically (`flex-col sm:flex-row` handles this)
- No horizontal overflow

- [ ] **Step 3: Check Skills on mobile**

Verify Skills section: three columns collapse to single column on mobile (`grid-cols-1 md:grid-cols-3` handles this). All cards readable.

- [ ] **Step 4: Check Projects on mobile**

Verify Projects section: three cards stack vertically on mobile. No text overflow.

- [ ] **Step 5: Check About on mobile**

Verify photo and text stack vertically (`flex-col sm:flex-row` handles this). Photo not oversized.

- [ ] **Step 6: Fix any layout issues found**

If any layout breaks are observed, fix the relevant component before proceeding.

- [ ] **Step 7: Stop server and run final build**

```bash
npm run build
```
Expected: clean build, no errors.

- [ ] **Step 8: Commit if any fixes were made**

```bash
git add -p
git commit -m "fix: mobile layout adjustments"
```

---

## Task 12: Final Build Verification + Deploy

**Files:** No new files.

- [ ] **Step 1: Run production build**

```bash
npm run build
```
Expected: `dist/` created, no warnings or errors.

- [ ] **Step 2: Preview production build**

```bash
npm run preview
```
Open `http://localhost:4321`. Walk through all five sections. Verify:
- All text content is correct (no placeholder text anywhere)
- Avatar photo loads
- Email link in Contact is `mailto:vincent.taijeron@gmail.com`
- `View My Work` button scrolls to Projects section
- `Get In Touch` button scrolls to Contact section
- Page title in browser tab reads: `Vincent "TJ" Taijeron — AI-Integrator`

Stop server.

- [ ] **Step 3: Check page source for SEO meta tags**

```bash
grep -A2 'meta name="description"' dist/index.html
grep 'og:title' dist/index.html
```
Expected: both meta tags present with correct content.

- [ ] **Step 4: Commit final state**

```bash
git add .
git commit -m "feat: complete site build — ready for deploy"
```

- [ ] **Step 5: Push to remote and verify Netlify deploy**

```bash
git push
```
Log into Netlify dashboard and verify:
- Build triggered automatically
- Build completes without errors
- Live URL renders the new site

---

## Definition of Done

- [ ] All five sections render correctly on desktop and mobile
- [ ] No placeholder text anywhere on the page
- [ ] Contact email links correctly
- [ ] Both Hero CTAs scroll to the correct sections
- [ ] Netlify deploy is green
- [ ] Page meta title and description are set
